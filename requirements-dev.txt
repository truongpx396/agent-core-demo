# Dev/CI-only tooling on top of the runtime deps — never installed in the
# Docker image (see Dockerfile), which installs requirements-lock.txt alone.
-r requirements.txt

ruff==0.16.4
mypy==2.3.1

# Parallel test execution (pytest -n auto — see Makefile's `test` target and
# tests/containers.py's own docstring for why this is safe against a suite
# that's otherwise 100% hermetic/mocked).
pytest-xdist==3.8.0

# Real, ephemeral Docker-backed services for tests/agent/test_durable_checkpoint.py,
# tests/integration/, and tests/live/ — see tests/containers.py. `[postgres,redis]`
# are dedicated testcontainers-python modules; Qdrant has no dedicated module as
# of this writing, so tests/containers.py drives it via the generic DockerContainer
# API instead (still part of the base `testcontainers` package). `ollama` is its
# own extra (testcontainers.ollama.OllamaContainer), used only by tests/live/.
testcontainers[postgres,redis,ollama]==4.9.0
# tests/containers.py's own cross-worker container reuse needs a real file
# lock, not a threading.Lock (workers are separate PROCESSES) — `filelock`
# is already a transitive dependency pinned in requirements-lock.txt
# (currently 3.32.4, pulled in by fastembed/huggingface_hub), so it's
# deliberately NOT re-pinned to a different version here: doing that
# once produced a real `pip install` dependency conflict (two different
# pins for the same package in one resolve) rather than a silent
# downgrade — caught in CI, not locally, since installing
# requirements-lock.txt and this file as two SEPARATE `pip install`
# invocations (as done manually while building this) doesn't trigger the
# same one-resolve conflict a single combined install (what CI actually
# runs) does.

# Browser E2E against app/api/static/index.html (tests/live/test_chat_ui.py).
# Python bindings, not the Node @playwright/test runner: this suite's E2E
# test shares one pytest session (and its testcontainers-backed services)
# with tests/live/'s Python-level Ollama tests — see GRAPH_PATTERNS.md
# pattern 48. Needs a one-time `playwright install --with-deps chromium`
# (see Makefile's `test-live` target), not installable via pip alone.
pytest-playwright==0.7.1

# LLM-judged RAG quality metrics (tests/live/test_rag_quality_deepeval.py,
# `@pytest.mark.deepeval`, `make deepeval` — manual only, never CI, see
# GRAPH_PATTERNS.md pattern 48). `ollama` is deepeval's own OllamaModel
# provider's runtime dependency (a plain `pip install deepeval` doesn't
# pull it — confirmed directly: OllamaModel.load_model() raises its own
# DeepEvalError telling you to install it, rather than failing at import
# time). Unlike garak (see garak/requirements-garak.txt), this does NOT
# need an isolated environment — pip's resolver DOES flag a conflict
# (deepeval pins `click<8.4.0`, huggingface_hub 1.29.0 here wants
# `>=8.4.2`) but verified directly this is a false alarm, not a real
# break like garak's langgraph-checkpoint one: huggingface_hub's own click
# usage is its unused `huggingface-cli` entry point, and `import
# huggingface_hub`/`fastembed`/this app's own `app.agent.graph` all work
# fine with the resulting click 8.3.x pip actually installs.
deepeval==4.2.0
ollama==0.6.2

# Load testing against the running FastAPI service's queued chat path
# (loadtest/locustfile_queued.py, `make loadtest-queued`/`make
# loadtest-queued-headless` — manual only, needs the real stack up, never
# CI). Pure-Python HTTP client fanout, no relation to
# garak's langgraph-checkpoint conflict or deepeval's click one above —
# verified directly: installing it alongside this file's existing pins
# introduces no NEW resolver conflict beyond the pre-existing deepeval/
# huggingface_hub click one already disclosed above.
locust==2.46.4
